<h1><%= @list.name %></h1>

<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= movie.title %>
      <%= movie.overview %>
      <%= movie.rating %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a new film", new_list_bookmark_path(@list), class: "btn btn-primary" %>
